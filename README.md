# SmartCan Backend

SmartCat is a device that plugs into the OBD-II connector from which it reads vehicle parameters such as speed, engine speed, fuel level, etc. 

The collected data, together with GPS coordinates, are transmitted via the mobile network to the server.

The user can view the vehicle's routes in the web application, and at each point view the parameters that were read from OBD-II.

## Description